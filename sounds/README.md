Put audio files in this directory. They can be
played from chat by using `!filename` (lowercase
and without the file extension). For example, if
`Giraffe.wav` was in this directory, it could be
played with `!giraffe`.
